    let users = [
        {id: 1, year: 1998, engine: 1, name: "Tico"},
        {id: 2, year: 2005, engine: 1.2, name: "Matiz"},
        {id: 3, year: 2010, engine: 1.6, name: "Gentra"},
        {id: 4, year: 2010, engine: 1.5, name: "Cobalt"},
        {id: 5, year: 2012, engine: 2, name: "Malibu"},
        {id: 6, year: 2000, engine: 1.2, name: "Damas"},
        {id: 7, year: 2018, engine: 2.4, name: "Tracker"},
    ];

// const oldYear = users.filter((v) => {
//     if(v.year < 2000) return false;
//     else return true;
// });
// console.log(oldYear);

// let newEngine = users.sort((a, b) => b.engine - a.engine);
// console.log(newEngine);

// let newYear = users.sort((a, b) => b.year - a.year);
// console.log(newYear);

// let newName = users.sort((a, b) => a.name.localeCompare(b.name));
// console.log(newName);

// 2000 < 
/* const oldCars = users.filter((v) => {
    if (v.year < 2000) return true;
    else return false;
})
console.log(`eski`, oldCars); */